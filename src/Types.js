import React from 'react'

export const Types = ({ types }) => {
  console.log(types)
  return (
    // <div className='types'>
    //     {
    //       types.map(type => {
    //         return <p>{type}</p>
    //       })
    //     }
    // </div>
    
    <div className='types'>{types}</div>
  )
}
